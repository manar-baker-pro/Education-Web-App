export interface ScreenShap {
  backgroundColor: string;
  height: string;
  width: string;
  borderRadius: string;
  top: string;
  right: string;
  zIndex: number;
}

export interface Signpara {
  error?: boolean;
  color?: string;
}
